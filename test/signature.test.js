describe('signature function', function() {

	it("should return a callable function", function() {
		expect(signature()).toBeFunction();
	});

	describe("with no arguments", function() {
		//sinon.mock(signature).expects('warn').notCalled

		//it("should ")
	});
});
